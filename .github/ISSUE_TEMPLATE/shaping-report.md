---
name: Shaping problem
about: Myanmar text renders wrongly with a built font (stacks, kinzi, medials, vowel marks)
labels: shaping
---

**Test text** (paste the exact Myanmar text that renders wrongly):

**What you see / what you expected** (a screenshot is worth a lot):

**Which font** (a gallery family, a release zip, or your own build — attach
the `.glyphstudio.json` if it's your own):

**Where it renders wrongly** (OS + app/browser — shaping differs between
platforms, so "Windows 11 / Word" vs "macOS / Safari" matters):

**Bonus for fast fixes:** the proof sheet from
`python pipeline/proof.py` on the font, or `hb-view`/`hb-shape` output
for the test text.
