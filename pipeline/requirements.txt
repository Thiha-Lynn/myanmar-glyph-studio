ufoLib2>=0.16
fonttools>=4.50
fontmake>=3.8
# fast, robust overlap removal for the stroke outlines fontmake compiles
skia-pathops>=0.8
# testing / proofing / sample generation (proof.py, make_sample.py)
uharfbuzz>=0.56.0
pillow>=10.0
# font QA (CI and local checks)
fontbakery>=0.12
pytest>=8
# WOFF2 web-font compression (fonttools ttLib.woff2)
brotli>=1.1
